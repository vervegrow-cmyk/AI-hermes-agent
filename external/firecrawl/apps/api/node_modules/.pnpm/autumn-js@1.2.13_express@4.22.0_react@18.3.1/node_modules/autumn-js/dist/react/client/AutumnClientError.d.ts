/** Error thrown by Autumn client */
declare class AutumnClientError extends Error {
    code: string;
    statusCode: number;
    details?: unknown;
    constructor({ message, code, statusCode, details, }: {
        message: string;
        code: string;
        statusCode: number;
        details?: unknown;
    });
}

export { AutumnClientError };
