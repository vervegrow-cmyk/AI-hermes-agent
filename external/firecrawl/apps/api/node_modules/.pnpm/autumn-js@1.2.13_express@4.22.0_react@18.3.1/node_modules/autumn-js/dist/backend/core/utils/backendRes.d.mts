import { BackendResult, BackendErrorBody } from '../types/responseTypes.mjs';

declare const backendSuccess: <T>({ body, statusCode, }: {
    body: T | null;
    statusCode?: number;
}) => BackendResult<T>;
declare const backendError: ({ message, code, statusCode, details, }: {
    message: string;
    code?: string;
    statusCode?: number;
    details?: unknown;
}) => BackendResult<BackendErrorBody>;
declare const isBackendResult: (value: unknown) => value is BackendResult;

export { backendError, backendSuccess, isBackendResult };
