import { A as AuthResult, R as ResolvedIdentity } from '../../authTypes-CyV4xBTB.mjs';
import { BackendResult } from '../types/responseTypes.mjs';

/** Result of identity resolution */
type IdentityResult = {
    success: true;
    identity: ResolvedIdentity;
} | {
    success: false;
    error: BackendResult;
};
/** Resolve identity from the getCustomer function and optionally validate */
declare const resolveIdentity: ({ getCustomer, requireCustomer, }: {
    getCustomer: () => AuthResult;
    requireCustomer?: boolean;
}) => Promise<IdentityResult>;

export { type IdentityResult, resolveIdentity };
