import { Request, RequestHandler } from 'express';
import { A as AuthResult } from '../authTypes-CyV4xBTB.mjs';

type ExpressAutumnHandlerOptions = {
    /** Function to identify the customer from the request */
    identify: (req: Request) => AuthResult;
    /** Autumn API secret key */
    secretKey?: string;
    /** Autumn API URL */
    autumnURL?: string;
    /** Path prefix for routes (default: "/api/autumn") */
    pathPrefix?: string;
};
declare function autumnHandler(options: ExpressAutumnHandlerOptions): RequestHandler;

export { type ExpressAutumnHandlerOptions, autumnHandler };
