import { NextResponse } from 'next/server';
import { A as AuthResult } from '../authTypes-CyV4xBTB.mjs';

type NextAutumnHandlerOptions = {
    /** Function to identify the customer from the request */
    identify: (request: Request) => AuthResult;
    /** Autumn API secret key */
    secretKey?: string;
    /** Autumn API URL */
    autumnURL?: string;
    /** Path prefix for routes (default: "/api/autumn") */
    pathPrefix?: string;
};
declare function autumnHandler(options: NextAutumnHandlerOptions): {
    GET: (request: Request) => Promise<NextResponse<unknown>>;
    POST: (request: Request) => Promise<NextResponse<unknown>>;
    DELETE: (request: Request) => Promise<NextResponse<unknown>>;
};

export { type NextAutumnHandlerOptions, autumnHandler };
