"use client";
import {
  useQuery
} from "./chunk-E5ZHOMZ4.mjs";
import {
  useAutumnClient
} from "./chunk-SOA7GEAN.mjs";

// src/react/hooks/useEntity.ts
var useEntity = (params) => {
  const client = useAutumnClient({ caller: "useEntity" });
  const { queryOptions, ...sdkParams } = params;
  const queryResult = useQuery({
    queryKey: ["autumn", "entity", sdkParams],
    queryFn: () => client.getEntity(sdkParams),
    ...queryOptions
  });
  return queryResult;
};

export {
  useEntity
};
